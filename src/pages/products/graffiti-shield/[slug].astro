---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import products from '../../../data/graffiti-shield-products.json';

export async function getStaticPaths() {
  return products.map((p) => ({
    params: { slug: p.slug },
    props: { product: p },
  }));
}

const { product } = Astro.props;

const relatedProducts = products.filter((p) => product.relatedSlugs.includes(p.slug)).slice(0, 3);

const title = `${product.name} | Salt Lake City Anti-Graffiti`;
const description = `${product.shortDescription} Authorized Graffiti Shield installer serving Salt Lake City, Downtown Salt Lake City, and the surrounding region.`.slice(0, 155);
const canonical = `https://windowfilmsaltlakecity.com/products/graffiti-shield/${product.slug}/`;

const faqItems = [
  {
    question: `What is ${product.name} window film?`,
    answer: product.description.split('\n\n')[0].replace(/<[^>]+>/g, '')
  },
  {
    question: `Where is ${product.name} film used in Salt Lake City?`,
    answer: product.description.split('\n\n')[1]?.replace(/<[^>]+>/g, '') || `${product.name} is used in Salt Lake City offices, retail storefronts, healthcare facilities, and residential properties throughout Downtown Salt Lake City and surrounding neighborhoods.`
  },
  {
    question: `How is ${product.name} film installed in Salt Lake City?`,
    answer: `${product.name} film is applied to the interior glass surface by Window Film Salt Lake City's authorized installation team. We assess glass type, dimensions, and application requirements before installation to ensure compatibility and long-term performance.`
  }
];

const pageSchemas = [
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
      { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://windowfilmsaltlakecity.com/" },
      { "@type": "ListItem", "position": 2, "name": "Products", "item": "https://windowfilmsaltlakecity.com/products/" },
      { "@type": "ListItem", "position": 3, "name": "Graffiti Shield Window Film", "item": "https://windowfilmsaltlakecity.com/products/graffiti-shield-window-film/" },
      { "@type": "ListItem", "position": 4, "name": product.name, "item": canonical }
    ]
  },
  {
    "@context": "https://schema.org",
    "@type": "WebPage",
    "url": canonical,
    "name": title,
    "description": description,
    "dateModified": new Date().toISOString().slice(0, 10),
    "breadcrumb": { "@type": "BreadcrumbList", "itemListElement": [] }
  },
  {
    "@context": "https://schema.org",
    "@type": "Product",
    "name": `${product.name} — Graffiti Shield Window Film`,
    "description": product.shortDescription,
    "brand": { "@type": "Brand", "name": "Graffiti Shield Inc." },
    "category": product.series,
    "offers": {
      "@type": "Offer",
      "seller": { "@type": "LocalBusiness", "name": "Window Film Salt Lake City" },
      "areaServed": "Salt Lake City, UT",
      "availability": "https://schema.org/InStock"
    }
  }
];



const specEntries = Object.entries(product.specs).filter(([, v]) => v && v !== 'null');
---

<BaseLayout {title} {description} {canonical} schemas={pageSchemas} faqItems={faqItems}>

<!-- ══ HERO ══ -->
<section class="gsp-hero">
  <div class="gsp-hero__bg" style={`background-image:url('${product.image}')`} role="img" aria-label={`${product.name} window film installed in Salt Lake City`}></div>
  <div class="gsp-hero__overlay"></div>
  <div class="gsp-hero__content">
    <div class="gsp-hero__eyebrow">
      <a href="/products/graffiti-shield-window-film/" class="gsp-hero__back">← Graffiti Shield Window Film</a>
      <span class="gsp-hero__eyebrow-dot" aria-hidden="true"></span>
      <span class="gsp-hero__series-pill" style={`background:${product.seriesColor}`}>{product.series}</span>
    </div>
    <h1 class="gsp-hero__title">{product.name}</h1>
    <p class="gsp-hero__sub">{product.shortDescription}</p>
    <div class="gsp-hero__chips">
      {product.specs.vlt && <span class="gsp-hero__chip">VLT: {product.specs.vlt}</span>}
      {product.specs.uvBlock && <span class="gsp-hero__chip">UV: {product.specs.uvBlock}</span>}
      {product.specs.application && <span class="gsp-hero__chip">{product.specs.application} application</span>}
      {(product.specs as any).thickness && <span class="gsp-hero__chip">{(product.specs as any).thickness}</span>}
    </div>
    <div class="gsp-hero__ctas">
      <a href="/contact/" class="gsp-hero__btn-primary">Get a Free Quote</a>
      <a href="tel:8018954681" class="gsp-hero__btn-secondary">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M20.01 15.38c-1.23 0-2.42-.2-3.53-.56-.35-.12-.74-.03-1.01.24l-1.57 1.97c-2.83-1.35-5.48-3.9-6.89-6.83l1.95-1.66c.27-.28.35-.67.24-1.02-.37-1.11-.56-2.3-.56-3.53 0-.54-.45-.99-.99-.99H4.19C3.65 3 3 3.24 3 3.99 3 13.28 10.73 21 20.01 21c.71 0 .99-.63.99-1.18v-3.45c0-.54-.45-.99-.99-.99z" fill="currentColor"/></svg>
        (801) 895-4681
      </a>
    </div>
  </div>
</section>

<div class="ip-page-body">

<!-- ══ OVERVIEW ══ -->
<section class="gsp-overview">
  <div class="gsp-overview__inner">
    <div class="gsp-overview__img-col">
      <img
        src={product.image}
        alt={`${product.name} window film installed in Salt Lake City`}
        title={`${product.name} — Solyx ${product.series} — Window Film Salt Lake City`}
        width="600" height="400"
        loading="lazy"
        class="gsp-overview__img"
      />
      <div class="gsp-overview__cats">
        {product.categories.map((c) => (
          <span class="gsp-overview__cat-pill">{c}</span>
        ))}
      </div>
    </div>
    <div class="gsp-overview__content">
      <span class="gsp-overview__eyebrow" style={`color:${product.seriesColor}`}>{product.series}</span>
      <h2 class="gsp-overview__title">About {product.name}</h2>
      {product.description.split('\n\n').map((para) => (
        <p class="gsp-overview__para">{para}</p>
      ))}
      <a href="/contact/" class="gsp-overview__cta">
        Request a site assessment &rarr; <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12h15m0 0l-6.75-6.75M19.5 12l-6.75 6.75"/></svg>
      </a>
    </div>
  </div>
</section>

<!-- ══ BENEFITS ══ -->
<section class="gsp-benefits">
  <div class="gsp-benefits__inner">
    <span class="pp-eyebrow">Key Benefits</span>
    <h2 class="gsp-benefits__title">Why specify {product.name} for Salt Lake City surfaces</h2>
    <div class="gsp-benefits__grid">
      {product.benefits.map((b) => (
        <div class="gsp-benefits__card">
          <div class="gsp-benefits__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none"><path d="M12 2l2.09 6.26H21l-5.47 3.97 2.09 6.27L12 14.54l-5.62 3.96 2.09-6.27L3 8.26h6.91L12 2z" fill="#81AB4C"/></svg>
          </div>
          <h3 class="gsp-benefits__heading">{b.heading}</h3>
          <p class="gsp-benefits__body">{b.body}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- ══ SPECS TABLE ══ -->
<section class="gsp-specs">
  <div class="gsp-specs__inner">
    <h2 class="gsp-specs__title">Technical Specifications</h2>
    <table class="gsp-specs__table" aria-label={`${product.name} technical specifications`}>
      <tbody>
        <tr class="gsp-specs__row"><td class="gsp-specs__label">Film Series</td><td class="gsp-specs__val">{product.series}</td></tr>
        <tr class="gsp-specs__row"><td class="gsp-specs__label">Brand</td><td class="gsp-specs__val">Graffiti Shield Inc.</td></tr>
        {specEntries.map(([k, v]) => (
          <tr class="gsp-specs__row">
            <td class="gsp-specs__label">{k === 'vlt' ? 'Visible Light Transmission' : k === 'uvBlock' ? 'UV Block' : k === 'application' ? 'Application' : k === 'warranty' ? 'Warranty' : k === 'thickness' ? 'Thickness' : k === 'heights' ? 'Standard Heights' : k === 'power' ? 'Power' : k === 'certification' ? 'Certification' : k === 'note' ? 'Note' : k}</td>
            <td class="gsp-specs__val">{v}</td>
          </tr>
        ))}
      </tbody>
    </table>
    <p class="gsp-specs__note">Source: Graffiti Shield Inc. product specifications (graffiti-shield.com). Contact Window Film Salt Lake City for product-specific technical documentation.</p>
  </div>
</section>

<!-- ══ APPLICATIONS ══ -->
<section class="gsp-apps">
  <div class="gsp-apps__inner">
    <h2 class="gsp-apps__title">Salt Lake City Applications</h2>
    <div class="gsp-apps__grid">
      {product.categories.map((cat) => (
        <div class="gsp-apps__card">
          <div class="gsp-apps__icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#81AB4C" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              {cat === 'Privacy' && <><rect x="3" y="3" width="18" height="18" rx="2"/><line x1="9" y1="3" x2="9" y2="21"/></>}
              {cat === 'Office' && <><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/></>}
              {cat === 'Healthcare' && <><path d="M22 12h-4l-3 9L9 3l-3 9H2"/></>}
              {cat === 'Hospitality' && <><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></>}
              {cat === 'Residential' && <><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/></>}
              {cat === 'Security' && <><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></>}
              {cat === 'Retail' && <><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/></>}
              {cat === 'Education' && <><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></>}
              {cat === 'Solar Control' && <><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/></>}
              {cat === 'Energy' && <><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></>}
              {cat === 'Custom' && <><circle cx="12" cy="12" r="3"/><path d="M19.07 4.93a10 10 0 0 1 0 14.14M4.93 19.07A10 10 0 0 1 4.93 4.93"/></>}
              {cat === 'Branding' && <><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></>}
              {cat === 'Decorative' && <><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></>}
              {cat === 'Luxury' && <><path d="M12 2l2.09 6.26H21l-5.47 3.97 2.09 6.27L12 14.54l-5.62 3.96 2.09-6.27L3 8.26h6.91L12 2z" fill="#81AB4C" stroke="none"/></>}
              {(cat === 'Themed' || cat === 'Seasonal') && <><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></>}
              {cat === 'Technology' && <><rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></>}
              {cat === 'Smart Glass' && <><rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></>}
              {cat === 'Environmental' && <><path d="M17 8C8 10 5.9 16.17 3.82 22M9.6 17C10.1 19.14 9.75 21.67 8 22"/><path d="M22 8s-4 8-14 8c-2 0-4-.5-5-1"/></>}
              {cat === 'Display' && <><rect x="2" y="3" width="20" height="14" rx="2"/><polyline points="8 21 12 17 16 21"/></>}
              {cat === 'Events' && <><path d="M12 2l2.09 6.26H21l-5.47 3.97 2.09 6.27L12 14.54l-5.62 3.96 2.09-6.27L3 8.26h6.91L12 2z"/></>}
              {cat === 'Construction' && <><polygon points="12 2 2 7 12 12 22 7 12 2"/><polyline points="2 17 12 22 22 17"/><polyline points="2 12 12 17 22 12"/></>}
              {cat === 'Transit' && <><rect x="2" y="7" width="20" height="13" rx="2"/><path d="M16 2H8a2 2 0 0 0-2 2v3h12V4a2 2 0 0 0-2-2z"/><circle cx="8" cy="16" r="1"/><circle cx="16" cy="16" r="1"/></>}
              {cat === 'Schools' && <><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></>}
              {cat === 'Religious' && <><path d="M18 11.5V22H6V11.5"/><path d="M2 10l10-9 10 9"/><path d="M12 22V12"/><path d="M9 15h6"/></>}
              {cat === 'Lighting' && <><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/></>}
              {cat === 'Photography' && <><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></>}
              {cat === 'Collaboration' && <><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></>}
              {!['Privacy','Office','Healthcare','Hospitality','Residential','Security','Retail','Education','Solar Control','Energy','Custom','Branding','Decorative','Luxury','Themed','Seasonal','Technology','Smart Glass','Environmental','Display','Events','Construction','Transit','Schools','Religious','Lighting','Photography','Collaboration'].includes(cat) && <><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></>}
            </svg>
          </div>
          <span class="gsp-apps__label">{cat}</span>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- ══ RELATED PRODUCTS ══ -->
{relatedProducts.length > 0 && (
  <section class="gsp-related">
    <div class="gsp-related__inner">
      <span class="pp-eyebrow">Also in the Graffiti Shield Lineup</span>
      <h2 class="gsp-related__title">Related Film Types</h2>
      <div class="gsp-related__grid">
        {relatedProducts.map((r) => (
          <a href={`/products/graffiti-shield/${r.slug}/`} class="gsp-related__card">
            <img src={r.image} alt={r.name} title={`${r.name} — Solyx ${r.series}`} width="400" height="267" loading="lazy" class="gsp-related__img" />
            <div class="gsp-related__body">
              <span class="gsp-related__series" style={`color:${r.seriesColor}`}>{r.series}</span>
              <h3 class="gsp-related__name">{r.name}</h3>
              <p class="gsp-related__desc">{r.shortDescription}</p>
            </div>
          </a>
        ))}
      </div>
      <div class="gsp-related__back">
        <a href="/products/graffiti-shield-window-film/" class="gsp-related__back-link">← View all Graffiti Shield products</a>
      </div>
    </div>
  </section>
)}

<!-- ══ CTA ══ -->
<section class="gsp-cta">
  <div class="gsp-cta__inner">
    <h2 class="gsp-cta__title">Install {product.name} in Salt Lake City</h2>
    <p class="gsp-cta__sub">Window Film Salt Lake City is an authorized Graffiti Shield installer. Get a free site assessment — no obligation, just expert advice on the right product for your surfaces.</p>
    <div class="gsp-cta__btns">
      <a href="/contact/" class="gsp-cta__btn gsp-cta__btn--primary">Get Your Free Estimate</a>
      <a href="tel:8018954681" class="gsp-cta__btn gsp-cta__btn--secondary">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M20.01 15.38c-1.23 0-2.42-.2-3.53-.56-.35-.12-.74-.03-1.01.24l-1.57 1.97c-2.83-1.35-5.48-3.9-6.89-6.83l1.95-1.66c.27-.28.35-.67.24-1.02-.37-1.11-.56-2.3-.56-3.53 0-.54-.45-.99-.99-.99H4.19C3.65 3 3 3.24 3 3.99 3 13.28 10.73 21 20.01 21c.71 0 .99-.63.99-1.18v-3.45c0-.54-.45-.99-.99-.99z" fill="currentColor"/></svg>
        (801) 895-4681
      </a>
    </div>
  </div>
</section>

</div><!-- end ip-page-body -->
</BaseLayout>

<style>
/* ══ HERO ══ */
.gsp-hero { position: relative; min-height: 70vh; display: flex; align-items: flex-end; overflow: hidden; padding-bottom: 64px; }
.gsp-hero__bg { position: absolute; inset: 0; background-size: cover; background-position: center; }
.gsp-hero__overlay { position: absolute; inset: 0; background: linear-gradient(to bottom, rgba(10,20,10,.2) 0%, rgba(10,20,10,.65) 60%, rgba(10,20,10,.88) 100%); }
.gsp-hero__content { position: relative; z-index: 2; width: 100%; max-width: 1400px; margin: 0 auto; padding: 0 6%; }
.gsp-hero__eyebrow { display: flex; align-items: center; gap: 12px; margin-bottom: 20px; }
.gsp-hero__back { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 13px; font-weight: 600; color: rgba(255,255,255,.75); text-decoration: none; transition: color .2s; }
.gsp-hero__back:hover { color: #fff; }
.gsp-hero__eyebrow-dot { width: 4px; height: 4px; border-radius: 50%; background: #81AB4C; }
.gsp-hero__series-pill { display: inline-block; padding: 4px 12px; border-radius: 999px; font-family: var(--font-body,'DM Sans',sans-serif); font-size: 11px; font-weight: 700; letter-spacing: .1em; text-transform: uppercase; color: #fff; }
.gsp-hero__title { font-family: var(--font-serif,'Playfair Display',serif); font-size: clamp(36px,6vw,72px); font-weight: 700; color: #fff; line-height: 1.05; letter-spacing: -1px; margin: 0 0 16px; max-width: 800px; }
.gsp-hero__sub { font-family: var(--font-body,'DM Sans',sans-serif); font-size: clamp(15px,1.8vw,18px); color: rgba(255,255,255,.85); line-height: 1.6; margin: 0 0 24px; max-width: 640px; }
.gsp-hero__chips { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 28px; }
.gsp-hero__chip { display: inline-flex; align-items: center; padding: 6px 14px; border-radius: 999px; background: rgba(255,255,255,.12); border: 1px solid rgba(255,255,255,.25); font-family: var(--font-body,'DM Sans',sans-serif); font-size: 12px; font-weight: 600; color: rgba(255,255,255,.9); backdrop-filter: blur(8px); }
.gsp-hero__ctas { display: flex; gap: 12px; flex-wrap: wrap; }
.gsp-hero__btn-primary { display: inline-flex; align-items: center; padding: 14px 28px; border-radius: 999px; background: #81AB4C; color: #fff; font-family: var(--font-body,'DM Sans',sans-serif); font-weight: 700; font-size: 15px; text-decoration: none; transition: background .2s, transform .2s; }
.gsp-hero__btn-primary:hover { background: #6e9640; transform: translateY(-2px); }
.gsp-hero__btn-secondary { display: inline-flex; align-items: center; gap: 8px; padding: 14px 24px; border-radius: 999px; background: rgba(255,255,255,.1); border: 1.5px solid rgba(255,255,255,.4); color: #fff; font-family: var(--font-body,'DM Sans',sans-serif); font-weight: 700; font-size: 15px; text-decoration: none; transition: background .2s; }
.gsp-hero__btn-secondary:hover { background: rgba(255,255,255,.18); }
/* ══ OVERVIEW ══ */
.gsp-overview { padding: 80px 6%; background: #fff; }
.gsp-overview__inner { max-width: 1280px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1.5fr; gap: 60px; align-items: start; }
.gsp-overview__img { width: 100%; height: 380px; object-fit: cover; border-radius: 8px; display: block; }
.gsp-overview__cats { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 16px; }
.gsp-overview__cat-pill { padding: 4px 12px; border-radius: 999px; background: #f0f5e8; border: 1px solid #c8dc98; font-family: var(--font-body,'DM Sans',sans-serif); font-size: 12px; font-weight: 600; color: #4a6a20; }
.gsp-overview__eyebrow { display: block; font-family: var(--font-body,'DM Sans',sans-serif); font-size: 11px; font-weight: 700; letter-spacing: .12em; text-transform: uppercase; margin-bottom: 12px; }
.gsp-overview__title { font-family: var(--font-serif,'Playfair Display',serif); font-size: clamp(24px,3vw,38px); font-weight: 700; color: #1a1a1a; line-height: 1.15; margin: 0 0 24px; }
.gsp-overview__para { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 15px; color: #3a3a3a; line-height: 1.75; margin: 0 0 16px; }
.gsp-overview__cta { display: inline-flex; align-items: center; gap: 6px; font-family: var(--font-body,'DM Sans',sans-serif); font-size: 14px; font-weight: 700; color: #81AB4C; text-decoration: none; margin-top: 12px; }
.gsp-overview__cta:hover { color: #6e9640; }
/* ══ BENEFITS ══ */
.gsp-benefits { padding: 80px 6%; background: #0d1a0d; }
.gsp-benefits__inner { max-width: 1280px; margin: 0 auto; }
.gsp-benefits__title { font-family: var(--font-serif,'Playfair Display',serif); font-size: clamp(22px,2.8vw,34px); font-weight: 700; color: #fff; margin: 8px 0 40px; }
.gsp-benefits__grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 24px; }
.gsp-benefits__card { background: rgba(255,255,255,.05); border: 1px solid rgba(255,255,255,.1); border-radius: 8px; padding: 28px 24px; }
.gsp-benefits__icon { margin-bottom: 16px; }
.gsp-benefits__heading { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 16px; font-weight: 700; color: #fff; margin: 0 0 10px; }
.gsp-benefits__body { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 14px; color: rgba(255,255,255,.72); line-height: 1.7; margin: 0; }
/* ══ SPECS ══ */
.gsp-specs { padding: 80px 6%; background: #f8faf4; }
.gsp-specs__inner { max-width: 720px; margin: 0 auto; }
.gsp-specs__title { font-family: var(--font-serif,'Playfair Display',serif); font-size: clamp(20px,2.5vw,30px); font-weight: 700; color: #1a1a1a; margin: 0 0 28px; }
.gsp-specs__table { width: 100%; border-collapse: collapse; }
.gsp-specs__row { border-bottom: 1px solid #e0e8d0; }
.gsp-specs__row:last-child { border-bottom: none; }
.gsp-specs__label { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 13px; font-weight: 700; color: #4a4a4a; padding: 14px 0; width: 45%; vertical-align: top; }
.gsp-specs__val { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 14px; color: #1a1a1a; padding: 14px 0; }
.gsp-specs__note { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 12px; color: #7a7a7a; margin-top: 20px; line-height: 1.6; }
/* ══ APPLICATIONS ══ */
.gsp-apps { padding: 80px 6%; background: #fff; }
.gsp-apps__inner { max-width: 1280px; margin: 0 auto; }
.gsp-apps__title { font-family: var(--font-serif,'Playfair Display',serif); font-size: clamp(20px,2.5vw,30px); font-weight: 700; color: #1a1a1a; margin: 0 0 32px; }
.gsp-apps__grid { display: flex; flex-wrap: wrap; gap: 16px; }
.gsp-apps__card { display: flex; flex-direction: column; align-items: center; gap: 8px; padding: 20px 24px; border: 1.5px solid #e0e8d0; border-radius: 8px; min-width: 120px; transition: border-color .2s, transform .2s; }
.gsp-apps__card:hover { border-color: #81AB4C; transform: translateY(-2px); }
.gsp-apps__icon svg { display: block; }
.gsp-apps__label { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 12px; font-weight: 600; color: #3a3a3a; text-align: center; }
/* ══ RELATED ══ */
.gsp-related { padding: 80px 6%; background: #f8faf4; }
.gsp-related__inner { max-width: 1280px; margin: 0 auto; }
.gsp-related__title { font-family: var(--font-serif,'Playfair Display',serif); font-size: clamp(20px,2.5vw,30px); font-weight: 700; color: #1a1a1a; margin: 8px 0 36px; }
.gsp-related__grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px,1fr)); gap: 24px; }
.gsp-related__card { border-radius: 8px; overflow: hidden; background: #fff; border: 1.5px solid #e0e8d0; text-decoration: none; transition: border-color .2s, transform .2s, box-shadow .2s; display: block; }
.gsp-related__card:hover { border-color: #81AB4C; transform: translateY(-3px); box-shadow: 0 8px 24px rgba(0,0,0,.1); }
.gsp-related__img { width: 100%; height: 180px; object-fit: cover; display: block; }
.gsp-related__body { padding: 20px; }
.gsp-related__series { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 11px; font-weight: 700; letter-spacing: .1em; text-transform: uppercase; display: block; margin-bottom: 6px; }
.gsp-related__name { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 16px; font-weight: 700; color: #1a1a1a; margin: 0 0 8px; }
.gsp-related__desc { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 13px; color: #5a5a5a; line-height: 1.6; margin: 0; }
.gsp-related__back { margin-top: 32px; }
.gsp-related__back-link { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 14px; font-weight: 700; color: #81AB4C; text-decoration: none; }
.gsp-related__back-link:hover { color: #6e9640; }
/* ══ CTA ══ */
.gsp-cta { padding: 80px 6%; background: #81AB4C; text-align: center; }
.gsp-cta__inner { max-width: 640px; margin: 0 auto; }
.gsp-cta__title { font-family: var(--font-serif,'Playfair Display',serif); font-size: clamp(24px,3.5vw,42px); font-weight: 700; color: #fff; margin: 0 0 16px; line-height: 1.1; }
.gsp-cta__sub { font-family: var(--font-body,'DM Sans',sans-serif); font-size: 16px; color: rgba(255,255,255,.88); line-height: 1.65; margin: 0 0 36px; }
.gsp-cta__btns { display: flex; align-items: center; justify-content: center; gap: 14px; flex-wrap: wrap; }
.gsp-cta__btn { display: inline-flex; align-items: center; gap: 8px; padding: 15px 30px; border-radius: 999px; font-family: var(--font-body,'DM Sans',sans-serif); font-weight: 700; font-size: 15px; text-decoration: none; transition: background .2s, transform .2s; }
.gsp-cta__btn--primary { background: #fff; color: #4a6a20; }
.gsp-cta__btn--primary:hover { background: #f0f5e8; transform: translateY(-2px); }
.gsp-cta__btn--secondary { background: rgba(255,255,255,.15); color: #fff; border: 1.5px solid rgba(255,255,255,.5); }
.gsp-cta__btn--secondary:hover { background: rgba(255,255,255,.25); transform: translateY(-2px); }
/* ══ RESPONSIVE ══ */
@media (max-width: 900px) { .gsp-overview__inner { grid-template-columns: 1fr; } }
@media (max-width: 600px) {
  .gsp-hero { min-height: 80vh; padding-bottom: 48px; }
  .gsp-hero__ctas { flex-direction: column; }
  .gsp-hero__btn-primary, .gsp-hero__btn-secondary { width: 100%; justify-content: center; }
  .gsp-benefits__grid { grid-template-columns: 1fr; }
  .gsp-cta__btns { flex-direction: column; }
  .gsp-cta__btn { width: 100%; justify-content: center; }
}
</style>
